<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android" android:color="?android:attr/colorControlHighlight">
    <item>
        <layer-list>
            <!-- Shadow -->
            <item android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/key_shadow" />
                    <corners android:radius="6dp" />
                </shape>
            </item>
            <!-- Key surface -->
            <item android:bottom="2dp" android:left="0dp" android:right="0dp" android:top="0dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/key_bg_color" />
                    <corners android:radius="6dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>
